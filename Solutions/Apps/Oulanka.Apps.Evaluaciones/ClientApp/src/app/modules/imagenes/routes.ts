import { ModuleWithProviders } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

export const imagenesRoutes: Routes = [
    
];
export const imagenesRouting: ModuleWithProviders = RouterModule.forChild(imagenesRoutes);